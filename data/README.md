# data

Place local source CSV files here when running the notebook outside Kaggle.

Expected files:

- `blood_donation_registry_ml_ready.csv`
- `blood_population_distribution.csv`
- `blood_compatibility_lookup.csv`

The notebook also supports a legacy `data/raw/` fallback, but this `data/` layout is the recommended local structure.
