# data/raw

Place the source CSV files here (not committed to the repository).

Required files:
- `blood_donation_registry_ml_ready.csv`
- `blood_population_distribution.csv`
- `blood_compatibility_lookup.csv`
- `data_dictionary.csv`

The notebook will use these local files if present, otherwise it falls back to the Kaggle input paths.
