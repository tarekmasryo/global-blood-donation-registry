# docs

Place the dataset documentation file here when running locally.

Expected file:

- `data_dictionary.csv`

On Kaggle, the notebook will resolve the attached dataset layout automatically when the same file is available under the dataset input directory.
